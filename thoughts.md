- remove logic that plays 5 rounds and replace it with buttons
- create events for each button 
- old code is a bit confusing so we will remove it doesn't work with what exists
    - getComputerChoice() function remains as is
    - human choice this function was removed it's serves no purpose now
    - logic for play round 
    - winner determination while winner is still gauged the same way the code needs to b    adjusted slightly 
    - already have variables for the respecitive scores 
    - create function that will take the result from calling play round and append
        repective score
    -DOM 
        - Output the winner
    
